{
  "name": "meshshield-ai",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "protocol:gen": "pnpm --filter @meshshield/protocol gen"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
